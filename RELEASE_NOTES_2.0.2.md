# PenBridge 2.0.2

iPad 연결이 붙자마자 바로 끊기고 무한 재연결되던 문제를 수정한 긴급 패치입니다.

## 수정 사항

- **iPad 연결이 즉시 끊기고 초당 여러 번 재연결을 반복하던 문제를 수정했습니다.**
  창의 로그·연결 상태 갱신을 UI 스레드로 넘기는 내부 처리(`PostIfOpen`)가, 서버(백그라운드 스레드)에서
  호출될 때 UI 전용 값을 먼저 읽어 예외를 던지고 있었습니다. 그 예외 때문에 서버가 방금 연결된 iPad 세션을
  스스로 종료했고, iPad는 곧바로 재연결을 시도해 무한 반복이 발생했습니다. 2.0.0의 WPF 전환 때 유입된
  회귀입니다.
- 이 증상을 "Wi-Fi 신호 문제"로 안내하던 README 문제 해결 항목을 함께 정리했습니다.

## 설치

`PenBridge-Setup-2.0.2.exe`를 실행하면 기존 설치를 같은 위치에서 업데이트합니다. Windows 10 버전 1809 이상
또는 Windows 11의 64비트 환경이 필요합니다.
