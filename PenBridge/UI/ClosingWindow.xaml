<Window x:Class="PenBridge.UI.ClosingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PenBridge" Width="340" Height="140" ResizeMode="NoResize"
        WindowStyle="ToolWindow" ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="종료 중…&#x0a;연결을 정리하고 있습니다. 잠시만 기다려 주세요."
                   TextWrapping="Wrap" TextAlignment="Center" VerticalAlignment="Center"
                   Foreground="{StaticResource TextPrimary}" FontSize="13"/>
        <ProgressBar Grid.Row="1" IsIndeterminate="True" Height="6" Margin="0,14,0,0"
                     Foreground="{StaticResource Accent}" Background="{StaticResource CardBorder}" BorderThickness="0"/>
    </Grid>
</Window>
